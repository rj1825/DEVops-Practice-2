apiVersion: v1
kind: Service
metadata:
  name: gitops-flask-service
  namespace: default
  labels:
    app: gitops-flask-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30080
  selector:
    app: gitops-flask-app
